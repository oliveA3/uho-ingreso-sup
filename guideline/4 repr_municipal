Header general que lleva al landing page

Sidemenu (abre automaticamente en el Dashboard) con:
    Dashboard -> 
        - conteo de escuelas, secretarios, directores (registros, activos)
        - lista de escuelas (nombre, cantidad de estudaintes, secretario, director, estado)
        - boton para agregar escuela (con su ventana modal)
    
    Usuarios -> 
        - lista de directores y secretarios (nombre, escuela, rol, correo, estado)
        filtrar por rol, provincia y estado
        - cada uno con botones para editar y eliminar
        - boton para agregar (con su ventana modal)
    
    Cerrar sesion ->